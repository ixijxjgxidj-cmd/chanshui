# 第31轮：INSTANCE 完整公开波形下载（进行中）

## 远程专用目录

`/root/5.6+chanshui1/public_round31`

## 文件清单

- URL：`http://repo.pi.ingv.it/instance/Instance_events_gm.hdf5.bz2`
- HTTP Content-Length：`161809684189` bytes
- ETag：`"25ac9bd6dd-5c10a3e15c0c0"`
- Accept-Ranges：`bytes`
- 下载方式：`curl -L -C - --retry 20 --retry-all-errors`
- 当前：后台断点续传，已在 2026-08-15 07:25 启动；2026-08-15 07:31 核验为 2,991,890,432 bytes（约 1.85%），速率约 8 MB/s，预计还需约 5.5 小时
- 远程磁盘：`/root` 可用约 1.1 TB；主下载进程 PID `1112310` 存活
- 临时文件不会进入 GitHub；仅提交元数据、脚本和校验记录。

## 后续协议

下载完成后先核验文件大小与 ETag，再顺序解压完整 HDF5；读取 metadata 中 `source_magnitude`、`trace_P_arrival_sample`、`station_network_code`，过滤 earthquake、ML/Mw、P±5 秒可用记录，按事件分组固定 train/dev，并构建远程 `t2_cache_instance31`。在公开 INSTANCE 区域留出与 STEAD 台站留出均通过前，不读取比赛数据。

## 合规

本轮只下载公开 INSTANCE；不读取 R1/R2/08 数据，不在本地训练或下载。

## 2026-08-15 07:36 状态
- 远程压缩波形：5,914,763,264 bytes（约 3.66%），速率约 8.2 MB/s，预计剩余约 5 小时 9 分。
- 下载进程仍为 PID 1112310；未启动任何比赛数据读取或训练。
- 旧轮27重复探针曾误启动，已立即终止，不产生新结论。

## 2026-08-15 07:38 状态
- 压缩波形：7,139,106,816 bytes（约 4.41%），下载 PID 1112310。
- 已启动远程完成触发器 PID 1113126：仅在严格达到 Content-Length 后执行 zip2 -dc，输出 Instance_events_gm.hdf5。
- 已核验 INSTANCE 元数据字段 115 列，后处理将按 source_type、ML/Mw、P±5 秒、事件级 split 与台站血缘过滤。

## 2026-08-15 08:33 状态
- 压缩波形：35,459,112,960 bytes（约 21.91%），主下载 PID 1112310 正常；自动解压守护仍等待严格完整文件。
- 远程可用空间约 893 GB，足够保留压缩包并解压至约 167 GB HDF5。
- 第37轮已审计已有 ETHZ/CREW/GEOFON/Iquique/OBST 池：仅含 T1 P/S 标签、无震级标签，不可错误用于 T2 跨域标定；INSTANCE 仍为 T2 的唯一正确公开跨域路径。

## 2026-08-15 08:44–08:46 状态
- 压缩波形：40,315,363,328 bytes（约 24.92%），下载与自动解压守护均存活；远程可用空间约 885 GB。
- 已从 SeisBench 0.12.3 源码核验：INSTANCE 完整 HDF5 使用 data/<trace_name> 键；官方 metadata 与 instance_split.csv 完整匹配（train 699,980 / dev 115,027 / test 344,242）。
- 已启动只读 HDF5 完整性核验守护 PID 1123957：解压后检查 20 条 metadata/HDF5 键、3×12000 形状和有限值；通过前不构建缓存。

## 2026-08-15 08:49 状态
- 压缩波形：43,115,126,784 bytes（约 26.65%）；下载 PID 1112310、解压守护 1113126、结构核验 1123957 均存活；远程余量约 878 GB。
- 已准备并通过语法审计 uild_instance_cache.py：仅官方 train/dev（test 永不读取），earthquake + ML/Mw + M3.8–6.5 + P±5s + 3×12000 有限值过滤，事件均匀抽样 train≤80k/dev≤20k，固定 seed 20260826。
- 已启动缓存构建触发器 1124086，等待完整性审计通过后自动执行。

## 2026-08-15 08:52 状态
- 下载进程 20 秒增长约 171 MB（约 8.6 MB/s），当前约 44.7 GB；未停滞。解压守护与核验/缓存触发器均处于等待状态。

## 2026-08-15 08:53 状态
- 压缩文件：44,991,160,320 bytes（约 27.81%）；下载、解压、核验、缓存触发器均正常。
- 远程可用空间约 884 GB；未启动任何新增训练，保持比赛数据零读取。

## 2026-08-15 08:53 后续核验
- 压缩文件继续增长至 45,312,352,256 bytes（约 28.00%）；尚未达到解压阈值，所有守护进程正常等待。

## 2026-08-15 08:55 状态
- 通过 Python/ls 核验实际压缩文件 46,030,888,960 bytes（约 28.45%）；20 秒速率约 8.40 MB/s。
- 注意远程 stat -c %s 在此环境输出 32 位截断值，后续统一用 Python os.path.getsize/ls --block-size=1 核验大文件。

## 2026-08-15 08:56 状态
- Python os.path.getsize 20 秒采样：46,402,707,456 → 46,576,148,480 bytes，速率 8.27 MB/s；当前完成约 28.78%。
- 下载与后继守护进程均存活，远程可用空间约 883 GB。

## 2026-08-15 08:57 状态
- 20 秒 Python 采样：46,907,236,352 → 47,078,842,368 bytes，速率 8.18 MB/s；当前完成约 29.10%。
- 下载、解压、HDF5 核验及缓存构建守护均正常；远程可用空间约 882 GB。

## 2026-08-15 08:58 状态
- 20 秒采样：47,399,608,320 → 47,578,980,352 bytes，速率 8.55 MB/s；当前约 29.42%。
- 下载、解压、核验、缓存构建守护均存活；远程可用空间约 882 GB。

## 2026-08-15 08:59 状态
- 20 秒采样：47,897,419,776 → 48,073,089,024 bytes，速率 8.38 MB/s；当前约 29.71%。
- 所有远程守护进程正常，余量约 881 GB。

## 2026-08-15 09:00 状态
- 20 秒采样：48,402,407,424 → 48,581,582,848 bytes，速率 8.54 MB/s；当前约 30.03%。
- 下载、解压、核验、缓存构建守护均正常；远程余量约 881 GB。

## 2026-08-15 09:00 后续状态
- 20 秒采样：48,894,582,784 → 49,069,039,616 bytes，速率 8.32 MB/s；当前约 30.32%。
- 下载、解压、核验、缓存构建守护正常；余量约 880 GB。
