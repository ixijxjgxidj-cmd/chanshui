# T2 第36轮：第34/35轮冻结候选的 R1/R2 train-only 一次性外部报告

## 合规声明

- 本轮只读 R1/R2 的 **train** 划分（各 140 条，来自 `outputs/t2_prereg_split.json`；脚本内断言 train∩holdout=∅）。
- **未读取** R1/R2 holdout（各 60 条，已在轮23完成唯一盲评，本轮零读取）。
- **未读取** `08-an.zip`、`08-exam.zip` 或任何 08 衍生产物。
- 所有参数在公开域（第34、35轮）已冻结；本轮不调整任何参数。
- 本地无训练、无下载；全部在远程 `/root/5.6+chanshui1` 执行。

## 重要合规限定（决定本报告的地位）

依据 `AGENTS.md` 第 2 条：第 1、2 轮数据**不得**用于训练、蒸馏、微调，也不得用于阈值选择、成员选择、超参选择、规则设计或失败原因分析，只允许作为**评估与不回归检查**。

因此本文件中的数值仅为一次性外部评估记录，**不构成**：

- 候选的录取或拒绝依据；
- 生产参数变更依据；
- 任何机制诊断或后续方法设计依据。

后续所有候选选择只依据独立公开跨区域数据。

## 冻结参数（进入本轮前已固定）

| 参数 | 值 | 来源 |
|---|---|---|
| `rho` | 0.6377 | 第35轮 8 折 `rho_hat` 中位数（公开 STEAD 台站 LOSO） |
| `lam_public` | 0.80 | 第34/35轮训练台站选出的公开常数 |
| `lam_prod` | 0.40 | 现役生产值 |
| `sigma_y` 估计式 | `0.5[(q90−q10)/2.563+(q75−q25)/1.349]` | 生产 `center_estimator_v2` 分位模型 |

## 记录值

### R1 train-only（n=140）

| 量 | 值 |
|---|---:|
| 估计中心 | 4.221 |
| 真实中位数 | 4.300 |
| 中心误差 | −0.079 |
| `sigma_z` | 0.3287 |
| `sigma_y_hat` | 0.4255 |
| `sigma_y` 真实 | 0.4949 |
| 自适应 λ | 0.825 |

| 配置 | score200 |
|---|---:|
| `λ=0.40` | 137.536 |
| `λ=0.80` | 134.412 |
| `λ=0.825`（自适应） | 134.017 |
| 常数@估计中心 | 134.837 |

### R2 train-only（n=140）

| 量 | 值 |
|---|---:|
| 估计中心 | 4.898 |
| 真实中位数 | 5.200 |
| 中心误差 | −0.302 |
| `sigma_z` | 0.3898 |
| `sigma_y_hat` | 0.3438 |
| `sigma_y` 真实 | 0.3688 |
| 自适应 λ | 0.562 |

| 配置 | score200 |
|---|---:|
| `λ=0.40` | 135.907 |
| `λ=0.80` | 128.426 |
| `λ=0.562`（自适应） | 133.869 |
| 常数@估计中心 | 133.666 |

## 生产状态

生产保持现状（`λ=0.40`、7 成员集成、GBM 批中心），本轮未做任何变更。变更只允许在独立公开跨区域验证给出满足预注册准则的候选之后进行。

## 后续合规路线

1. INSTANCE 完整公开波形到位后，建立 STEAD↔INSTANCE 双向跨区域留出，作为候选选择的唯一依据。
2. 公开域标定必须同时留出台站与训练分布：仅留出台站的协议不足以标定与逐记录判别力相关的量。该约束由公开域自身的设计原理得出，不依赖本轮外部数值。
3. R1/R2 的本次记录不再参与任何后续选择；其 holdout 不再读取。

## 证据文件（远程）

- `/root/5.6+chanshui1/outputs/t2_round36_trainonly.json`
- `/root/5.6+chanshui1/outputs/t2_round36_trainonly.log`
- 脚本 `/root/5.6+chanshui1/eval36_trainonly.py`
