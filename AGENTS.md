# 项目级硬约束

1. **08 决赛测试数据集永远不得参加任何实验。** `08-exam.zip`、`08-an.zip` 及由它们生成的波形、答案、特征、缓存、预测、残差或其他衍生产物，不得用于训练、蒸馏、微调、调参、阈值选择、特征选择、消融、误差诊断、候选筛选、早停、规则设计或失败原因分析，也不得根据其历史结果继续修改方法。
2. **第 1、2 轮比赛数据永远不得参与训练、蒸馏或微调。** `第1轮比赛试题与答案_*.zip`、`第2轮比赛试题与答案_*.zip` 及由它们生成的窗口池、特征、标签或权重，均不得进入任何训练、蒸馏、微调，也不得用于阈值选择、成员选择或超参选择。它们只允许用作**评估与不回归检查**，且必须同时报告四种数量罚口径。
   - 依据：用户目标语句把四个包（`08-an.zip`、`08-exam.zip`、第 1 轮、第 2 轮）全部列为「测试数据集永远不参与模型微调与训练」。本文件此前写有「第 1、2 轮……可按预注册协议参与实验」，该句由本项目自身提交（`d1d7d52`→`75c17c6`）加入，与用户指令冲突，**以用户指令为准**，现已删除。
   - 已识别的历史违规资产见 `memory/CURRENT_STATE.md`「合规缺陷」节：`weights/aug/exam_aug6_r2train_sd.pt`、`weights/aug/crew_sp23_r2train_sd.pt`、`weights/official_r1_to_r2/t2_seismicxm_r1r2.joblib`、`t3_seismicxm_r1r2.joblib`、`t2_seismicxm_r1.joblib`、`t3_seismicxm_r1.joblib`、`t2_magnitude_baseline.joblib`、`t3_event_baseline.joblib`。它们保留为历史冻结证据，但**不得进入任何新发布清单**。
3. **允许联网获取公开可访问的数据集用于实验，不以是否明确声明许可证作为准入条件。** 必须记录来源、可获得的校验和与固定的数据划分，若来源提供许可证或使用条款则一并记录。训练集和验证集应按事件或其他防泄漏分组隔离。
4. **本地不做训练、微调与数据集下载。** 训练与数据获取只在远程算力上进行，且必须使用与其他项目隔离的专用目录。