# 项目级硬约束

1. **08 决赛测试数据集永远不得参加任何实验。** `08-exam.zip`、`08-an.zip` 及由它们生成的波形、答案、特征、缓存、预测、残差或其他衍生产物，不得用于训练、蒸馏、微调、调参、阈值选择、特征选择、消融、误差诊断、候选筛选、早停、规则设计或失败原因分析，也不得根据其历史结果继续修改方法。

2. **第 1、2 轮比赛数据允许用于训练，但必须留出验证集。** `第1轮比赛试题与答案_*.zip`、`第2轮比赛试题与答案_*.zip` 可用于训练、蒸馏、微调，但必须按事件ID或其他防泄漏分组划分train/val，确保验证集完全独立。同时必须用作**评估与不回归检查**，且报告四种数量罚口径。
   - 更新依据：用户 2026-08-14 明确「r1和r2能用于训练，08不能用于训练，r1r2用于训练需要留出部分测试数据集」。
   - 历史违规资产（在R1/R2禁训期间产生）见 `memory/CURRENT_STATE.md`「合规缺陷」节，保留为证据但不进入新发布。

3. **允许联网获取公开可访问的数据集用于实验，不以是否明确声明许可证作为准入条件。** 必须记录来源、可获得的校验和与固定的数据划分，若来源提供许可证或使用条款则一并记录。训练集和验证集应按事件或其他防泛漏分组隔离。

4. **本地不做训练、微调与数据集下载。** 训练与数据获取只在远程算力上进行，且必须使用与其他项目隔离的专用目录。
