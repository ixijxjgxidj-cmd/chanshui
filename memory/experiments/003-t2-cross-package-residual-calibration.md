# 实验 003：T2 源包 OOF 残差的低维局部校准

- 日期：2026-08-11
- 轮次：03
- 状态：**实验完成；开发阶段严格失败，未读取 08，拒绝进入生产**
- 基准分支：`main`
- 基准提交：`3cfabf7`（实验实现前 HEAD；生产代码仍为 `53117b9`）
- 论文证据：`memory/papers/round-03-t2-cross-package-calibration.md`

> 本文件在实验代码与结果生成之前冻结假设、配置网格、选择协议和成功/失败阈值。运行后只能追加结果与结论，不得根据 R2/R1 或 08 标签回改门槛。

## 1. 本轮目标

检验以下可证伪问题：

> 只使用训练源包的交叉拟合预测和 OOF residual，在“基线预测值 + 极低维 PCA”邻域内做强收缩残差修正，能否在不接触目标标签、目标均值或目标批次统计的条件下，同时降低 R1→R2 与 R2→R1 的 MAE 和有符号偏差？

本轮不是端到端重训，不调用新的 SeismicXM 编码器，不改变 T1/T3，也不重复历史失败的 quantile/Huber/双模型平均。

## 2. 冻结数据与基准

开发缓存：

- 路径：`outputs/seismicxm_t3/features_tta.npz`
- SHA-256：`ce213a04472713bb9f76667f62d9132ae0fa08539a9945058534759b746ea574`
- 必需键：`X1t2/y1t2/X2t2/y2t2`
- 形状：两包各 `200 × 1024`

冻结基准：

```text
Pipeline(StandardScaler(), Ridge(alpha=30.0))
prediction clip: [0.0, 9.9]
```

已核验基准值：

| 方向 | MAE | RMSE | signed bias (`prediction - truth`) |
|---|---:|---:|---:|
| R1→R2 | `0.621046` | `0.742015` | `-0.589283` |
| R2→R1 | `0.660726` | `0.756001` | `+0.539461` |

08 终检缓存已经存在，但开发阶段不得加载。其 SHA-256 只用于终检前身份核对：

`357b851dce6352648b0fadf169715ca6e47f0c170c078b4d5241fae97607d8d4`

冻结生产 08 T2 结果：MAE `0.523197455`，signed bias `+0.099781905`。

## 3. 假设

### 主假设

虽然源包 OOF 总体 signed bias 接近 0，但 residual 可能随基线预测值和少数低维表征方向呈现稳定的回归收缩结构。交叉拟合 residual 能避免用训练内残差制造虚假可预测性；强收缩局部估计可能修正一部分跨包偏差，而不需要目标批次。

### 反假设

跨包偏差主要是标签先验/条件分布变化、绝对振幅信息丢失或源/目标整体截距变化。此时 source-only OOF residual 不包含目标偏移方向；局部校准只能在包内看似改善，跨包无效或反向恶化。

## 4. 严格禁止的信息流

配置选择、PCA、距离阈值、邻域 residual、修正强度和是否启用候选均不得读取：

- 目标包标签；
- 目标包标签均值、方差或直方图；
- 目标包整批特征的均值、协方差、PCA 或密度比；
- 08 特征或 08 标签（开发通过前）；
- 根据跨包结果回调的常数偏移。

允许输入只有：

- 源包特征与标签；
- 单个待预测目标样本的特征；
- 源包内部交叉拟合产生的 OOF 基线预测和 residual。

代码接口必须把“源包拟合”和“目标无标签预测”分离，测试中验证目标标签没有参数入口。

## 5. 候选机制与固定网格

### 5.1 源包 OOF residual

在每个训练源包内部使用 `5` 折、shuffle、固定随机种子生成 OOF 预测：

```text
residual_i = y_i - ridge_oof_prediction_i
```

每个样本的 OOF 预测必须来自没有见过该样本标签的 Ridge。

### 5.2 低维校准空间

校准空间由两部分组成：

1. 源包 OOF 基线预测值（始终存在）；
2. 源包特征经 `StandardScaler + PCA(whiten=True)` 得到的 `0/8/16` 个分量。

最终拟合时，源样本使用 OOF 基线预测；目标样本使用“在完整源包拟合的 Ridge”预测。PCA、标准化器和预测值标准化参数均只在源包拟合。

为避免 `PCA=0` 时单一标量在 cosine 归一化后退化为正负两个点，所有校准向量固定附加一个常数 `1` 的齐次坐标。该坐标不含标签或目标信息、不参与网格，也不改变“预测值 + 0/8/16 个 PCA 分量”的可学习信息边界。

### 5.3 局部 residual 估计

- 校准向量最后做 L2 归一化；距离为 cosine distance。
- 邻居数 `k ∈ {20, 40}`。
- 权重固定为 `1 / (distance + 0.05)`。
- 局部 residual 为邻居 residual 的加权均值。
- 修正强度 `shrinkage ∈ {0.25, 0.50}`。
- 局部 residual 先截断到 `[-0.20, +0.20]`，再乘 shrinkage。
- 最终预测仍 clip 到 `[0.0, 9.9]`。

固定激活候选共 `3 × 2 × 2 = 12` 个；另有不修正的 Ridge 基线。实验后不得扩展 PCA 维数、k、shrinkage、截断幅度或距离权重网格。

### 5.4 源包距离闸门

对源残差库逐样本 leave-one-out，计算其 `k` 邻居平均 cosine distance；固定取源包分布的 `90%` 分位为阈值。目标单样本平均邻域距离高于该阈值时，回退到 Ridge 基线。

该闸门只控制候选覆盖率，不被描述为已证明的误差概率。输出必须报告：

- 阈值；
- 目标覆盖率；
- 覆盖内/外 MAE；
- 邻域距离与绝对误差的 Spearman 相关（仅诊断，不用于选型）。

## 6. 嵌套源包选择协议

对 R1、R2 分别独立执行 `5` 次重复 × `5` 折外层验证：

1. 外层训练折内再做 5 折交叉拟合，生成 residual 库。
2. 所有 scaler/PCA/距离阈值只在外层训练折拟合。
3. 外层验证折只用于产生未见预测。
4. 聚合 25 个外层折后，按 MAE 选择配置。

防止“微小噪声收益”规则：

- 若最优激活配置相对源包外层 Ridge 的 MAE 改善小于 `0.005`，该源包选择基线，不启用修正。
- 候选 MAE 相差不超过 `0.001` 时，按固定复杂度顺序选更简单者：PCA `0 < 8 < 16`，`k=40 < k=20`，shrinkage `0.25 < 0.50`。
- 不能用另一包的跨包标签改变本源包选择。

配置稳定性通过条件：

- R1 与 R2 都选择激活修正；
- 两者 shrinkage 相同；
- PCA 维数最多相差一个相邻档（允许 `0↔8` 或 `8↔16`，不允许 `0↔16`）；
- k 可在 `20/40` 间不同。

任一源包回退基线、shrinkage 不同或 PCA 选择跨两档，均记为“机制选择不稳定”，开发判定失败。

## 7. 双向开发验证与成功门槛

源包选型完成后：

- 用 R1 选出的配置在完整 R1 上拟合，预测 R2；
- 用 R2 选出的配置在完整 R2 上拟合，预测 R1；
- 跨包标签只用于最终评估，不用于配置、门控或参数拟合。

开发阶段必须同时满足：

1. R1→R2 MAE 相对 Ridge 改善至少 `0.010`；
2. R2→R1 MAE 相对 Ridge 改善至少 `0.010`；
3. 两方向平均 MAE 改善至少 `0.015`；
4. 两方向 `abs(signed bias)` 均严格下降；
5. 任一方向不得恶化；
6. 两个源包外层 OOF MAE 均不得相对 Ridge 恶化超过 `0.010`；
7. 配置稳定性通过；
8. 不增加编码器调用、TTA 次数或主要生产延迟。

只有全部满足，`development_pass=true`，才允许进入 08 一次性终检。任何单项失败都立即锁定 `terminal_08_allowed=false`。

## 8. 08 一次性终检协议

只有开发通过时执行：

1. 核对终检缓存 SHA-256；
2. 在 R1/R2 各自 12 个配置的源包 OOF 表上，选择“平均 OOF delta 最低”的共同配置；并列时沿用第 6 节复杂度顺序；
3. 在合并的 R1+R2 上重新生成 5 折 OOF residual 库并拟合该共同配置；
4. 对 08 预测一次，记录逐样本误差；
5. 不得根据 08 结果换配置、改截断、改 gate 或加常数。

08 采用门槛：

- MAE `≤ 0.513197455`，即相对冻结 `0.523197455` 至少改善 `0.010`；
- `abs(signed bias) ≤ 0.099781905`；
- 结果不是固定标签均值或固定包间偏移；
- 定向测试、全量测试与 bundle/API 回归全部通过。

08 未通过则整轮拒绝，不进入生产。

## 9. 输出与可复现性

计划实现：

- `scripts/experiment_t2_residual_calibration.py`
- `tests/test_t2_residual_calibration.py`
- 默认输出：`outputs/experiments/round03_t2_residual_calibration.json`（受 `.gitignore` 保护）

固定随机种子：`20260811`。

JSON 至少记录：

- 输入缓存路径与 SHA-256；
- Python/NumPy/scikit-learn/平台；
- 12 个固定配置；
- R1/R2 外层每折基线与候选预测；
- 每包选择结果、OOF MAE、signed bias、gate coverage；
- 双向逐样本 truth/base/candidate/residual/distance/gate；
- MAE、RMSE、signed bias、Spearman；
- 配置稳定性与全部开发判定原因；
- 是否加载 08；
- 若获准，终检缓存哈希、共同配置和逐样本结果；
- 运行时间。

## 10. 预期成本与生产边界

- 只读取已缓存的 1024 维向量；不调用 SeismicXM，不需要 GPU。
- 样本量最多 400，PCA 和邻域查询为秒到分钟级。
- 若通过，额外生产成本只是至多 400 个、最多 17 维参考向量的距离计算；不得增加模型编码次数。
- 若失败，只提交研究、实验脚本、测试和负结果，不改 bundle/API，不同步或重启服务器。
- 若通过终检，也必须先实现可关闭开关、bundle 版本和回滚测试，才有资格部署。

## 11. 已知历史负结果与“本轮为何不是重复”

不得原样重复：

- GBR quantile：R1→R2 `0.7464`、R2→R1 `0.7066`；
- Huber：`0.7164/0.7587`；
- QuantileRegressor：最高维小样本下退化；
- 双 Ridge 简单平均：08 `0.5509`；
- 相同绝对幅值/手工特征拼接：弱于 deep1024；
- 根据 08 `+0.09978` 直接减固定常数。

本轮实质不同之处：

1. residual 来自严格交叉拟合，不是训练内残差；
2. 修正是源包内部、低维、局部、强收缩，不替换 Ridge 主模型；
3. 每个方向的配置仅由其源包 OOF 选择；
4. 双向跨包是 08 之前的硬门槛；
5. 失败时不读取 08，避免终检反复调参。

## 12. 失败后的下一步

若开发失败：

- 将本方向记入 `memory/failed-experiments.md`；
- 不扩大同类 PCA/k/shrinkage 网格；
- 不用 R1/R2 跨包标签事后挑配置；
- 不读取 08；
- 保持生产 Ridge `alpha=30`；
- 下一高价值工作转向旧文档漂移/生产鲁棒性，或等待 DiTing 数据后建立带绝对幅值、位置/距离和按包留出的新 T2 训练阶段。

若开发通过但 08 失败：同样拒绝生产，并记录“跨包双向可迁移但未通过冻结终检”，不得继续针对 08 调参。

## 13. 实现与测试

实际新增：

- `scripts/experiment_t2_residual_calibration.py`
- `tests/test_t2_residual_calibration.py`

实现与预注册一致：

- 12 个固定配置，没有在看到结果后扩网格；
- 源包 5×5 重复嵌套 OOF 选择；
- OOF Ridge residual、齐次坐标、余弦邻域、`90%` 源距离闸门；
- residual 截断 `±0.20`、shrinkage `0.25/0.50`；
- `fit_source_and_predict` 接口没有目标标签参数；
- `run` 只有在 `development_pass` 后才计算 08 文件哈希并打开终检缓存。

定向测试首次运行：

```text
python -m py_compile scripts/experiment_t2_residual_calibration.py tests/test_t2_residual_calibration.py
python -m pytest -q tests/test_t2_residual_calibration.py
8 passed in 1.78s
```

测试覆盖固定网格、交叉拟合折隔离、低维几何、距离闸、局部 residual 数值方向、目标标签接口隔离、双向准入门槛，以及“开发失败时即使终检路径不存在也不得尝试打开”的防泄漏边界。

## 14. 实际运行与确定性

执行命令（终检本地路径脱敏）：

```text
python scripts/experiment_t2_residual_calibration.py \
  --features outputs/seismicxm_t3/features_tta.npz \
  --terminal-features <frozen-08-t2-cache> \
  --output outputs/experiments/round03_t2_residual_calibration.json \
  --seed 20260811 \
  --outer-repeats 5
```

环境：

- Python `3.13.3`
- NumPy `2.5.2`
- scikit-learn `1.9.0`
- Windows 11 x64

完整开发运行两次；除 `runtime_seconds` 外，JSON 深层比较完全相同：

```text
substantive_equal=true
runtime=5.6096s / 5.6355s
```

第二次冻结输出 SHA-256：

`919034107bbd855541a0f9ba33ec828edbd6c782ff93e296e61a0242f7b861c3`

输出位于被 `.gitignore` 保护的 `outputs/experiments/`，不提交逐样本历史答案或本地路径。

## 15. 源包嵌套 OOF 选择结果

两个源包都独立、稳定地选择同一个配置：

```text
pca0_k40_s50
PCA dimensions = 0
k = 40
shrinkage = 0.50
```

这意味着 8/16 维 SeismicXM PCA 没有给出比“只按 Ridge OOF 预测值定位邻域”更强的包内 residual 信号。

| 源包 | 重复外层 Ridge MAE | 选中候选 MAE | 改善 | gate coverage | signed bias（基线→候选） |
|---|---:|---:|---:|---:|---:|
| R1 | `0.293562` | `0.275316` | `+0.018247` | `91.3%` | `-0.001160 → -0.008624` |
| R2 | `0.222057` | `0.213599` | `+0.008458` | `90.7%` | `-0.007782 → -0.005050` |

R2 的增益仍超过预注册的源包启用线 `0.005`，所以两个方向都按协议启用了候选；配置稳定性通过。这里的“包内上涨”只是进入跨包反证的条件，不是录取证据。

## 16. 双向跨包开发结果

| 方向 | Ridge MAE | 候选 MAE | MAE 改善 | Ridge signed bias | 候选 signed bias | gate coverage |
|---|---:|---:|---:|---:|---:|---:|
| R1→R2 | `0.621046` | `0.621532` | `-0.000485` | `-0.589283` | `-0.589620` | `90.0%` |
| R2→R1 | `0.660726` | `0.669618` | `-0.008892` | `+0.539461` | `+0.552312` | `93.0%` |

双向平均改善为 `-0.004689`，即平均反而恶化约 `0.00469 MAE`。预注册要求两个方向各改善至少 `0.010`、平均改善至少 `0.015`，而实际两个方向均为负。

候选平均修正方向也直接暴露问题：

| 方向 | 应有的全局纠偏方向 | 实际平均 correction | correction 范围 | 正/负/零样本数 |
|---|---|---:|---:|---:|
| R1→R2 | 应整体上调 | `-0.000337` | `[-0.06065, +0.10000]` | `62/118/20` |
| R2→R1 | 应整体下调 | `+0.012851` | `[-0.05928, +0.08130]` | `91/95/14` |

也就是说，源包 OOF 学到的局部“回归到均值”修正，在目标包上平均恰好没有提供所需的包间截距方向，R2→R1 甚至进一步把预测均值从 `5.003461` 推高到 `5.016312`。

距离诊断同样不能救回候选：

- R1→R2 距离与候选绝对误差 Spearman 为 `-0.41736`，距离更大并不代表误差更大；
- R2→R1 为 `0.07911`，近似无相关；
- gate 覆盖 `90–93%`，没有隔离出足够明确的跨包危险区；
- R2→R1 的 gate-off 14 个样本候选 MAE 较低只是事后分组诊断，不得据此改阈值或反向选择覆盖率。

## 17. 预注册判定

`development_pass=false`。失败项为：

1. R1→R2 改善 `-0.000485 < 0.010`；
2. R2→R1 改善 `-0.008892 < 0.010`；
3. 双向平均改善 `-0.004689 < 0.015`；
4. 两个方向绝对 signed bias 都未下降；
5. 两个方向 MAE 都恶化。

通过项只有：

- 两个源包都达到 `0.005` 的包内启用线；
- 两个源包选择完全相同，配置稳定性通过；
- 源包 OOF 没有恶化；
- 无额外编码器/TTA 成本。

准入是全部条件的合取，不能用包内上涨覆盖跨包失败。

## 18. 08 防泄漏核验

结果 JSON 明确记录：

```text
terminal_08.allowed=false
terminal_08.evaluated=false
terminal_08.feature_cache=null
terminal_08.feature_cache_sha256=null
```

虽然运行命令传入了终检路径，代码在 `development_pass=false` 后没有计算该文件哈希、没有调用 `np.load`、没有读取 08 特征或标签。第二次确定性复跑也保持同一边界。

因此没有 08 MAE、没有共同配置、没有 bundle，也没有任何根据 08 结果进行的二次选择。

## 19. 失败解释与结论

结论：**拒绝 source-only OOF residual 的预测值/PCA 局部校准；生产继续使用 `StandardScaler + Ridge(alpha=30)`。**

本轮证据支持反假设：包内 residual 中确实存在可重复的回归收缩结构，但它不包含目标包整体标签/条件分布偏移的方向。包内校准可以降低同包 OOF MAE，却不能推断目标包需要整体上调还是下调；强收缩和距离回退也没有解决这一信息缺失。

不得进行的事后挽救：

- 不检查 12 个配置在目标标签上的跨包排名；
- 不把 gate 90% 分位改成事后更有利的覆盖率；
- 不按 R1/R2 标签均值差加入固定截距；
- 不读取 08 挑选 `pca/k/shrinkage`；
- 不扩大相同 PCA/kNN residual 网格。

只有出现以下实质新证据时才值得重开：

- 合法、代表正式分布的无标签目标批次，可预注册 KMM/CORAL 类方法；
- 站点、区域、仪器、距离或绝对幅值等能解释偏移的元数据；
- DiTing/目标区带标签数据，可做按包留出的端到端或 transfer learning；
- 新的独立包证明偏移可由 source-only 不变量预测。

## 20. 生产与下一步

- 不改 T2 bundle、API 或默认参数；
- 不同步实验脚本到生产发布目录；
- 不重启当前稳定服务；
- 本轮代码只保留为可复现的否证工具和防重复证据；
- 下一轮不再扩大 T2 轻量头网格，转向现有数据仍能验证的最高价值 T1/生产鲁棒性问题，或先清理会误导发布的旧文档漂移。

GitHub 状态：主结果提交 `57e28f1` 已推送 `origin/main`。预注册提交为 `10f9773`，余弦齐次坐标的实验前澄清为 `acad92d`。
