# 决策日志

## 2026-08-11：生产与实验分轨

- 决策：当前七成员服务继续作为生产候选运行；提分实验先在本地冻结评估中完成，不直接改线上参数。
- 理由：公网服务已经稳定，当前最大风险来自规则口径、复现性和历史调参泄漏，而不是服务未上线。
- 回滚：线上仍可回到提交 `47fb72c` 对应的 systemd 配置与七成员资产。

## 2026-08-11：所有 T1 候选必须报告四种数量罚口径

- 决策：默认 `merged_file_floor0` 仅用于与历史成绩连续比较，不再作为唯一录取标准。
- 录取原则：优先四口径方向一致；至少要求最坏可信口径不出现有意义退化，并解释口径敏感性。
- 触发复审：组委会给出新的书面算例或评分代码。

## 2026-08-11：不自动积分加速度

- 决策：保留输入原始物理量，不根据通道码自动执行加速度到速度积分。
- 证据：官方群确认“数据不限制加速度记录，以速度为主”；08 两个 HN 文件上的积分变体均劣于原始输入。
- 后续：若取得可靠仪器响应与单位元数据，可另立预注册实验，不在主链路做启发式转换。

## 2026-08-11：缺口屏蔽先量化、后上线

- 决策：把 `Waveform.gaps` 未被 picker 消费列为第一优先实验，但在完成历史包统计和预注册前不改生产行为。
- 录取条件：合成缺口测试通过；无缺口预测逐位一致；四包或可用历史包最坏口径不退化；屏蔽窗宽有物理与模型感受野依据。

## 2026-08-11：08 T3 不再称“五类盲测”

- 决策：保留 `183/205` 这一包内准确率，但明确答案只出现标签 1–4。
- 理由：第 5 类能力应由第 1 轮或未来真正包含标签 5 的独立数据验证，不能由未出现该类的包证明。

## 2026-08-11：DiTing 原始数据不作为当前阻塞项

- 决策：原始数据预计下周才能取得；当前三轮实验使用现有包、合成压力测试与公开可得数据/论文证据推进。
- 禁止：为了等待 DiTing 而停止基线、规则、缺口、匹配和 T2/T3 校准工作。

## 2026-08-11：T1 与 T2/T3 使用不同的短窗下限

- 决策：`/pick` 和任何依赖 picks 的估计器继续拒绝 `<5s`；`needs_picks=False` 的 T2/T3 固定窗模型允许任意正长度输入。
- 直接证据：第 1 轮官方 T3 有 6 个 1.5–3.5 秒第 5 类样本；旧 API 全部空响应，生产 SeismicXM 对六条均正确分类。
- 收益：第 1 轮有效 T3 accuracy 由旧 API 91.0% 恢复到 94.0%；第 5 类 recall 40%→100%。
- 安全边界：不是全局关闭时长检查；只有不依赖 PhaseNet picks、具备自身固定窗补齐的模型可绕过 5 秒下限。

## 2026-08-11：拒绝 T3 中心化/等类原型头，不消耗 08 终检

- 决策：保留生产 `Normalizer + cosine kNN(k=5)`；拒绝本轮训练折中心化、NCM/top-m/多原型及 R1 margin/support 门控，不构建 bundle、不部署。
- 证据：候选在第 1 轮嵌套验证的 balanced accuracy 从 `0.865636` 升至 `0.908364`，但第 1→2 轮从基线 `187/189` 崩至 `121/189`；门控覆盖第 2 轮 91.5%，仅恢复到 `130/189`。
- 解释：训练包均值中心化显著放大域移；即使移除中心化，对应 raw top-m 最好也只有 `182/189`，仍未达到预注册 `186/189`。包内 margin/support 不能当作跨包 OOD 检测器。
- 防泄漏边界：`development_pass=false` 后不提取、不查看 08 T3 特征；不得用第 2 或 08 标签事后挑一个 raw 配置再宣称通过本轮。
- 重开条件：出现实质不同的域不变机制、可靠无标签域移信号或新的独立验证包。

## 2026-08-11：拒绝 T2 source-only OOF residual 校准，不消耗 08 终检

- 决策：生产继续使用 `StandardScaler + Ridge(alpha=30)`；拒绝预测值/低维 PCA 邻域上的源包 OOF residual 强收缩校准，不构建 bundle、不改 API、不部署。
- 证据：R1/R2 源包重复嵌套 OOF 分别改善 `0.018247/0.008458 MAE`，且都选择 `pca0_k40_s50`；但 R1→R2 从 `0.621046` 恶化到 `0.621532`，R2→R1 从 `0.660726` 恶化到 `0.669618`，两个方向绝对 signed bias 均扩大。
- 解释：源包 residual 能学习同包回归收缩，却不包含目标包整体截距/条件分布漂移的方向；距离闸覆盖 `90–93%`，也没有形成可靠 OOD 风险信号。
- 防泄漏边界：`development_pass=false` 后没有计算终检缓存哈希、没有打开 08 T2 特征；不得用 08 或跨包标签事后改 PCA/k/shrinkage/gate 或加入固定包均值差。
- 重开条件：出现合法无标签目标批次、解释偏移的元数据、目标区/DiTing 标签或新的独立验证包。

## 2026-08-11：第 4 轮只测试长记录完整低可信 P/S 事件删除

- 决策：固定现有 5 成员长记录推理、`-1 dB` SNR 闸和 20 秒同相位去重；只在 `>300s` 后处理层测试 FIFO `0.2–60s` P→S 配对与几何联合 confidence 阈值 `{0.35,0.40,0.45}`。
- 理由：R2 两个长文件与 08 五个长文件分别集中约 `23.5%/39.4%` 的 T1 损失；多数额外拾取能组成完整假 P/S 对，事件联合 confidence AUC 约 `0.7616`，继续扩大固定 dedup 窗已有真实事件合并负结果。
- 防重复边界：短文件 P/S 联合重选在 R2/08 仅 `+0.0/+0.1`，本轮所有 `≤300s` 输出必须逐位不变；不得删除全部 orphan、重扫 `long_dedup_s` 或新增事后阈值。
- 录取：R2/08 各自源包选型、leave-one-file-out 稳定、双向跨包和较低 tau 共同候选必须同时通过；四种数量罚方向一致，逐文件不得新增 FN、降低 P/S 时差分或降低文件分数。
- 生产边界：当前只冻结研究与预注册，尚未运行阈值评分；只有正向结果、全量回归和可关闭开关完成后才允许部署。

## 2026-08-11：拒绝长记录 FIFO 事件 confidence 删除

- 决策：保留生产 20 秒同相位去重；拒绝 tau `0.35/0.40/0.45` 的 FIFO 完整 P/S 事件几何 confidence 删除，不增加生产开关、不改 API、不部署。
- 基线可信度：冻结缓存、答案和基线哈希匹配；OFF 路径 7 文件计数、逐文件残差/FP/FN/时差分/文件分及两包四种完整口径均在 `1e-9` 内复现。
- R2 证据：三档最坏归一化增益均为负。tau 0.35 将 FP `126→87`，但 FN `37→56`，卷级两口径各下降 `18.0944`；更高阈值更差。
- 08 证据：tau 0.35 的四种完整包分数均上涨 `+9.0944～+16.0944`，但 FN `96→116`，4/5 个长文件新增 FN或时差分下降；这是数量罚聚合掩盖真实相位损失，不能录取。
- 稳定性：R2、08 full-source 与全部 LOFO 子集都选择 OFF，因此没有 active 双向配置和共同 tau，`development_pass=false`。
- 安全与成本：短文件/300 秒边界/候选子集/确定性均通过，后处理 P95 约 `0.2048 ms`；拒绝原因是选择性不足，不是实现或性能问题。
- 防重复边界：不得扩阈值、改变 60 秒、换 min/max/加权 confidence 或按 08 事后加 veto。只有独立长记录包、显式 event/noise 分支、可解释校准或多台站/走时证据才可重开。

## 2026-08-11：生产发布必须通过机器可校验清单

- 决策：`deploy/production_release_manifest.json` 是生产默认参数、跟踪资产、外置 encoder、验收提交与回滚提交的单一发布清单；部署脚本必须在写 unit 或重启服务前运行校验器。
- 默认安全策略：T2/T3 默认选择 SeismicXM 时，外置 encoder 缺失必须中止部署；只有明确设置 `ALLOW_MODEL_FALLBACK=1` 才允许缺失时降级。文件存在但大小或 SHA-256 不符时，无论是否允许 fallback 都必须失败。
- 理由：旧脚本会在缺 encoder 时继续启动 baseline，容易把“服务可用”误当成“生产模型已就绪”；静态默认值与资产哈希联合校验可以在重启前发现配置漂移和错误文件。
- 隐私边界：manifest、文档和模型元数据不得保留服务器、账号、代理、私钥或比赛数据机器绝对路径。历史 checkpoint/joblib 只清洗元数据，必须验证张量或预测不变。
- 部署边界：本轮没有改变生产推理输出，稳定服务器不因发布工具和文档硬化而重启；新闸门在下一次正向发布时生效。

## 2026-08-11：拒绝最终 Pick 列表固定 margin 缺口屏蔽

- 决策：不在所有阈值、条件式 force-pair、SNR 与 dedup 完成后按 `Waveform.gaps` 固定区间删除最终 picks；margin `0/0.5/1/2/5/10s` 全部拒绝，不改 `PickerConfig`、API、manifest 或部署。
- 实现可信度：7 成员、三包与权重哈希、77/77 注入身份、无 gap 对象身份、single/batch、重复性全部通过；10,000 picks/100 gaps/500 次 P95 `2.1463 ms`。拒绝原因不是代码或性能。
- 数据证据：77 个开发变体产生 31 个 induced、36 个 lost；13 个 induced 和 2 个 lost 距物理 gap 超过 10 秒。`margin=0` 残留 23 个 induced；`margin=10s` 仍残留全部 13 个远程 induced，并 collateral delete 37 个稳定 reference picks。
- 防终检泄漏：开发没有 active margin，因此没有运行 08 波形推理；ignored JSON 中 `holdout08.records=null`。
- 解释：gap 已在更上游改变概率、阈值峰或 force-pair 上下文，最终删除层不能恢复远程丢失，也不能删除远程诱发。不得扩大 margin、自适应选窗或在同轮改成 taper/interpolation。
- 重开边界：只允许把作用点前移到 annotation/正常阈值/force-pair 之前，重新检索新论文并独立预注册；若 gap 10 秒外 probability/final picks 仍变化则同样拒绝。

## 2026-08-11：拒绝 P/S annotation 局部固定 guard，冻结 zero-fill 后处理方向

- 决策：不在七成员 annotation 平均后、正常阈值与 conditional force-pair 前按 gap±`0/0.5/1/2/5/10s` 把 P/S probability 局部置零；六档全部拒绝，不改生产、不部署。
- 实现可信度：三包/权重/七成员身份、77/77 注入、annotation 对齐、五条 annotation→生产 Pick 零差复刻、66/66 no-gap 对象身份、重复性全部通过；400,000×2 样点、100 gaps、200 次 P95 `1.6621 ms`。拒绝原因不是实现或性能。
- 概率证据：754,070 个 gap 10 秒外 P/S 样点中，295,900 个变化超过 `1e-6`，最大差 `0.609977`；正常阈值与 `0.03` floor 分别穿越 745/3,551 次。
- 峰与最终输出：normal remote peak 为 `12 induced / 2 lost`，floor 为 `22 / 38`；raw final 精确复现第 5 轮的 `31 induced / 36 lost / 13 remote induced / 2 remote lost`。77/77 变体 probability 条件失败，28/77 峰条件失败，13/77 最终 Pick 条件失败。
- guard 诊断：0 秒已恶化为 `33 residual / 40 lost`；10 秒仍有 `18 residual`，并产生 `77 lost / 37 collateral`。局部 veto 不能回溯修复卷积/滑窗已造成的远程变化。
- 防终检泄漏：没有 active guard，选择 `OFF`；08 波形未运行，ignored JSON 保持 `holdout08.records=null`。
- 稳定边界：不再扩大固定 guard，不按文件/相位/gap 长度自适应，不在相同 zero-fill 输出上改 NaN、taper 或 interpolation。只有真实 gap/独立 gap 包、可冻结 gap augmentation 训练划分、模型层 observation mask 或可靠滑窗贡献重算机制出现时重开。

## 2026-08-11：watchdog 跳过采集必须使用双重认证，cron 固定由 root 唯一管理

- 决策：只有直接 TCP 对端为数值型 loopback，且请求 token 与服务器 0600 文件中的随机值恒定时间匹配时，才允许完整推理后跳过捕获并返回 accepted；XFF/X-Real-IP 永不授权。
- 服务器证据：认证回环、部署烟测、手动 watchdog 与真实 5 分钟 cron 的捕获/manifest 增量均为 `0`；公网伪造 token 与回环代理头仍产生恰好 `1` 条捕获。
- cron 边界：条目安装在 root crontab，使用唯一 managed marker，数量必须为 `1`。回滚前必须先撤下，前滚全部验收通过后才能恢复。
- 临时日志边界：禁止固定 `/tmp` 日志名。Linux sticky-dir 会阻止 root 覆盖其他用户创建的普通文件，导致成功探活误判并重启；提交 `baa6f77` 固定使用每次 `mktemp` 的私有日志和 EXIT 清理，创建失败时 fail closed。
- 隐私边界：token 值不得出现在仓库、unit、进程参数、cron 或 journal；只允许在服务器运行时文件中存在。

## 2026-08-11：生产回滚采用“先停 cron、detach 旧提交、验收后再前滚”

- 决策：回滚前保存当前 unit、提交和 root crontab，先停用 watchdog cron；使用 `git switch --detach` 切到已知旧提交并恢复旧 unit，不使用 `reset --hard`。
- 已验证行为：旧 unit 不含 probe 参数，token 文件仍以 `0600` 保留但不被旧服务或 cron 使用；旧版四端点均 200，启动无 fallback。
- 捕获治理：回滚测试按普通业务请求进入捕获，必须使用唯一文件名/哈希逐条核验并精确归档，不能用目录级删除；本次 3 条记录全部清理后生产捕获为 0。
- 再前滚：切回 `main` 后必须重跑正式部署脚本、确认 token 被复用、三业务端点 accepted 且不采集，再恢复唯一 root cron并观察真实定时 OK。
- 结果：回滚与再前滚均通过，最终服务器为 `main/baa6f77`、服务 active+enabled、cron 数量 1、生产捕获为空。

## 2026-08-11：最终冻结复评通过，保持七成员/SeismicXM 生产版本不变

- 决策：冻结当前 T1 七成员、T2/T3 SeismicXM 模型与全部生产参数；第 2–6 轮已否证方向不因冻结检查重新打开。
- 离线证据：提交 `5d014b4` 上重新解析 3,894 个 MSEED，T1 三包、08 T2/T3 完整结果与旧冻结输出逐字段一致；结构画像仅运行时延迟不同。最终结果仍为 T1 `1.7862944444 / 1.8101396478 / 2.0100836168`、08 T2 MAE `0.5231974550`、08 T3 `183/205`。
- 公网证据：51.5 秒真实三分量代表样本的四端点均 HTTP 200，T1 为 1P/1S，T2/T3 非空且模式合法；公网响应不含 probe accepted 标记。
- 数据治理：三份真实样本捕获均精确核验后删除，原始本地归档仍可恢复；服务器仅保留不含波形的审计元数据，生产 manifest 与捕获波形最终为 0。
- 重开边界：只有新独立数据、官方规则实质变化或预注册实验取得跨分布可复现净收益时，才允许改变生产推理；运维自动化可继续加固但不得隐式改模型。

## 2026-08-11：服务器发布固定使用仓库级 GitHub 只读 deploy key

- 决策：服务器只部署当前 GitHub 仓库的只读 deploy key，不部署个人账号或任何具有写权限的凭据；私钥只存在于服务器运行环境，权限固定为 `0600`。
- 主机信任：GitHub SSH 主机公钥只取自官方 metadata API，追加到默认 `known_hosts` 并保留已有内容；仓库本地 SSH 命令强制 `IdentitiesOnly=yes` 与严格主机校验，不全局影响其他仓库。
- 发布方式：服务器远端只允许 GitHub SSH；发布固定为 `git fetch` 后 `git merge --ff-only`，禁止用 `reset --hard` 覆盖服务器状态。`ls-remote` 与两阶段纯快进构成发布证据。
- 权限证据：GitHub 端 deploy key 为只读，服务器非真实写入 dry-run 被拒绝；后续不得为了方便把本地或个人写权限凭据复制到服务器。
- 服务边界：仅文档或证据提交的 Git 快进不触发服务重启；只有生产代码、配置或资产真正变化且通过发布清单校验时，才进入部署与重启流程。

## 2026-08-11：发布后出现的非探针公网样本先保留再判定

- 事实：发布链最终零捕获审计完成后，服务器新增 1 条 `/pick` 记录；它包含 1 个 217 字节项，来源不是 loopback，且文件名不带探针/烟测标记。生产环境 ObsPy 复核确认它不是合法 MiniSEED，API 响应为空对象。
- 决策：保留该样本及其 manifest 元数据作为畸形公网输入的鲁棒性审计证据，不纳入训练或评分数据；不得把“当前目录非零”直接当成 watchdog 污染，也不得用目录级删除掩盖事实。
- 边界：只有逐文件确认属于本次测试且有可恢复原始归档时，才允许精确归档并删除；真实公网数据继续按正式捕获治理。

## 2026-08-11：历史冻结分数保留贪心匹配以维持连续性

- 决策：默认评分器暂不切换匹配算法；历史基线继续使用现有贪心实现，以保持预测哈希和既有分数逐位连续。
- 证据：独立 Hungarian 审计对 2,699 个历史 T1 文件同时运行最大官方时差分、最大匹配数后最小总残差两种精确目标，P/S 的逐文件得分、总分和匹配数与贪心全部一致；第 2 轮 2 个与 08 5 个多事件文件也无差异。
- 非泛化边界：合成密集反例中精确分数比贪心高 `1/9`，因此不能宣称贪心普遍最优。新密集包或官方匹配说明出现时必须重新审计，若差异实质影响候选排序，再把多口径精确结果纳入评分报告。
- 生产边界：本审计只增加离线脚本和测试，不改 picker、API、模型、阈值或服务器运行服务。

## 2026-08-12：拒绝所有已有 T1 候选四口径稳健替换

- 决策：继续使用生产 `g7`；不把 `base/cond/fp/g6/g6gate/ov90/prod2` 中任何一个接入生产或改变提交参数。
- 录取规则：三包 × 四种数量罚共 12 个完整包单元必须全部不低于 `g7`，且至少一个严格上升；覆盖率必须完整。该规则同时检查卷级和按文件口径，不允许用单个正向包或卷级数量罚上涨覆盖逐文件时差/FP/FN 损失。
- 服务器证据：权威 JSON 绑定 `79028e0`，SHA-256 为 `98353140ad1f0958d2caab60aa92c4ec8e1bf229b0085cead2cb838da140f988`；7 个候选全部 `robust_pass=false`。最接近的 `ov90` 最差单元 `-6.150000000001`，12 单元均值 `-3.097222222223`，仅 2/12 正向。
- 失败结构：`ov90` 第 2 轮两个卷级口径各上涨约 `0.8611`，但第 1 轮四口径全降、第 2 轮按文件两口径降、08 四口径全降；默认口径下三包合计 467 个文件受损，P/S 时差分与 FP/FN 也出现逐包回归。
- 生产边界：服务器全量测试在固定仓库 cwd 下 `352 passed`，服务仍 active、健康检查 200；审计只增加脚本、测试与 memory，不改 picker、API、模型、阈值、manifest 或捕获治理。
- 重开条件：新独立数据、官方规则实质变化，或完全不同且预注册后 12 单元均不下降的新候选；不得继续在同一 8 组历史输出上事后挑包、挑口径或重排。

## 2026-08-12：CPU PhaseNet 蒸馏可行，进入正式三折 LOPO

- 决策：继续使用同架构 `PhaseNet(diting)` 做冻结七成员教师蒸馏，不切换到 LogisticRegression/小 MLP 概率特征学生；下一阶段生成真实平均概率缓存并训练三折 out-of-package 学生。
- 计算证据：绑定提交 `a846c26` 的服务器权威 JSON SHA-256 为 `7a6a46473d05d5f78fcd2440db8c1643f46142ba839c25782ceaece22b6e43be`。全量教师缓存预计 `0.036702h`，平均 float16 N/P/S 约 `65,137,386` 字节；学生 CPU 平均 step `0.008471s`，最慢单折 10 epoch 预计 `0.055274h`，峰值 RSS `702,156,800` 字节。
- 完整性证据：三包 2,699 个 T1 文件全部盘点；代表 annotation 均包含 N/P/S 三通道、50 Hz 概率网格和正确长记录长度；学生 loss `0.302985→0.216054`，确认 forward/backward/optimizer step 真实发生。
- 预注册：只比较 `KD-only` 与 `0.7 KD + 0.3 hard`；学生、10 epochs、AdamW `1e-4`、batch 2、30 秒 stride、P/S Gaussian sigma `0.2/0.3s` 固定，不做温度、alpha、epoch、成员权重或后处理阈值网格。
- 防泄漏边界：严格整包 LOPO；held-out 包不参与训练、早停或候选选择。最终必须运行生产完整后处理，同时报告三包 × 四数量罚 12 单元、逐文件回归、FP/FN、P/S 时差分和长记录；12 单元全部不下降且至少一项严格提升才可录取。
- 服务器边界：当前服务器只用于训练、特征生成和实验计算，不作为最终部署服务器；跳过 systemd、cron、服务重启、捕获权限、生产发布与运维加固。

## 2026-08-12：拒绝基础 PhaseNet LOPO 蒸馏替换，保留域稳健研究信号

- 决策：`KD-only` 与 `KD+hard` 均不替换生产 `g7`，六份学生 checkpoint 不部署，不修改 picker、阈值、成员权重或后处理。
- 工程证据：正式 float16 N/P/S 缓存覆盖 2,699 个文件，实际 `466.721s`；六次固定 10 epoch 训练约 181–225 秒，六次 held-out 推理约 195–229 文件/秒。held-out 包和答案在训练阶段保持关闭。
- 准入证据：权威审计绑定 `f508480`，JSON SHA-256 为 `ff98f656505e213f55c70a951ad4abdc8a07631b00d360d014cf35806d28b77d`。`KD-only` 12 单元最差/均值为 `-10.4111/-2.9944`；`KD+hard` 为 `-6.2722/+6.3491`，但第 1 轮四单元全降，因此两者均 `robust_pass=false`。
- 解释边界：`KD+hard` 在 R2、08 和全部 7 个长记录上上涨，且 R2/08 同时减少 FP/FN、提高 P/S 时差分，说明硬标签包含可利用信号；但不能按包挑模型或用 8/12 正向覆盖 R1 回退。
- 禁止事后调参：不得在相同三包上继续扫描 KD/hard 权重、温度、epoch、early stopping、阈值、overlap、成员权重或后处理，也不得按包选择 checkpoint。
- 重开条件：先用新的 AnySearch MCP + Browser MCP 原始论文轮次解释软硬标签的跨包不一致，再预注册一个实质不同的域稳健训练机制；服务器仍只做离线实验，不做最终部署。

## 2026-08-12：拒绝包—记录层级等权 KD+hard，冻结同三包权重变体

- 决策：`package-record-balanced KD+hard` 不替换生产 `g7`，三份学生 checkpoint 不部署，不修改 picker、阈值、成员权重或后处理。
- 工程证据：绑定 `4edc4c5` 的新教师缓存覆盖 2,699 文件，耗时 `474.718s`，与旧缓存逐记录 SHA-256/shape/成员数完全相同；三折固定 10 epoch 训练约 189–227 秒，权重均值 1、包总权重相等、包内记录总权重相等检查全部通过，held-out 始终关闭。
- 准入证据：权威审计 JSON SHA-256 为 `8ffe1f5cd419cdcaaad4066cc3e3036569fb01bd324ba6c7e26d7a807ad5f009`。12 单元 worst/mean 为 `-8.6889/-3.3602`，仅 2/12 正向，`robust_pass=false`。
- 失败结构：R1 默认口径回退从基础 `KD+hard` 的 `-6.2722` 缩小到 `-2.3222`，但 R2/08 反转为 `-5.1944/-8.6889`；R2/08 分别新增 `12/19` FP。7 条长记录仅 1 条上涨，合计 `-14.7222`。
- 解释边界：完全层级等权确实改变跨包行为，但不能证明存在可用的连续权重折中；长记录低权重与 FP 退化只是一致性诊断，不授权事后增加权重下限。
- 重开条件：不得在同一三包继续扫描包权、记录权、截断、下限、长度分段或 KD/hard/epoch/阈值。优先等待真正独立的新包或官方规则变化；若无新数据，停止围绕相同蒸馏风险做连续变体。
