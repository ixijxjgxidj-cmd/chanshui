# 训练与提分计划表（2026 第二届"震智杯"·API 赛制）

> **2026-08-01 依据官方《比赛说明》与报名页情报全面改版。** 第二届赛制与去年真题
> 形态差异很大（长连续记录 / 强震仪 / 震级 API / 多轮评分），旧版计划整体移入文末
> 留档区；其中已完成项（P0 保底上线本地侧、阈值与合并窗搜索）的结论继续有效。
> 凡下文标注"**待官方确认**"的条目一律不当既成事实用。

## 0. ⚠ 用户行动项（最高优先，阻塞 P1 / P2 / P4）

> **请登录 earthquake.ac.cn 大赛页完成三件事：**
>
> 1. **下载"赛题 1 样例数据"**——官方已提供样例，仓库侧推理 / 微调 / 评测管线全部
>    就绪，唯一缺口就是这份数据；
> 2. **确认震级 API 的输入输出格式**——报名页有"震相 API"+"震级 API"两个登记字段，
>    震级输出格式官方未公布；
> 3. **拿到评分细则原文**——到时分档、数量罚口径今年是否沿用去年，直接决定阈值策略。
>
> 官方页拿不准的，进赛事 QQ 群 **1009806591** 求证。
>
> 另有 P0 遗留行动项：云主机仍未到位（阿里云/华为云 CPU 2~4 vCPU 按量付费即可），
> 到手后 `git clone` + `sudo bash deploy/deploy_api.sh` 一条龙，见 `deploy/README.md`。

## 1. 第二届赛制实况（2026-08-01 情报汇总）

| 项 | 情报 | 对策 / 影响 |
|---|---|---|
| 硬 deadline | **2026 年 9 月初决赛**，距今约 5 周 | 所有排期倒排；大改动只放前 3 周，后 2 周只做定装与运维 |
| 评分方式 | 公网 API 自动评分；**第一届打了六轮**，今年大概率同样多轮（具体轮数**待官方确认**） | API 必须**长期稳定在线**（不是"开放一天"）；轮与轮之间是迭代窗口 → P5 |
| 数据形态 | **长连续记录**（官方示例约 28 分钟）；单条含多事件多 P/S，也可能零事件；100Hz miniseed；文件名如 `T1.E001.mseed` | 已实测：28 分钟 12 事件合成长记录，全默认管线 12/12 P + 12/12 S 全中，单文件 0.16s——链路无恐慌，但真实样例域必须复测（P1） |
| 仪器混合 | 除测震速度计外，含**强震仪加速度计（HN? 通道）** | 加速度域是现有基座没重点覆盖的形态，P1 域复测 + P2 样例微调覆盖 |
| 计分单位 | 按"**震相总数**"计，约 2000 个 | 数量罚暴露面与去年"按文件"口径不同；阈值/合并窗在样例上复核（P1） |
| 评分细则 | 去年口径：P ≤0.1s 满分 / 1s 零分线性；S ≤0.2s / 2s；数量罚 −0.5/超 1 个。**今年数值与口径待官方确认** | 细则原文到手前，调参一律按去年口径 + 多读法稳健准则（`outputs/p3_grid/结论.md` 方法论） |
| 震级 API | 报名页独立登记字段；第一届主题就含震级预测 → **按真赛项准备**；输出格式**待官方确认** | 保底端点先行（/magnitude），模型升级排 P4 |
| 竞争格局 | 约 60 队；去年前排普遍 = 业务预训练 + 赛题微调 | 我们同路线：diting（业务级预训练）起点 + 官方样例微调（P2） |
| 奖项梯度 | 一、二等奖合计仅前 4 名 | 保 API 稳定 = 保底名次；冲前 4 只能靠微调 + 集成的真实分差 |

**已实测底气**（全部在去年两轮真题 / 合成长记录上验证，出处 `outputs/评审报告_20260801.md` 与 `outputs/p3_grid/结论.md`）：

- 当前全默认配置（diting + P=0.2/S=0.15 + 合并窗 P1.0s/S3.0s，单一真源
  `src/phasepicker/defaults.py`）：去年第 1 轮 **1.732**/2.0，第 2 轮 **1.669**/2.0。
- CPU 速度不是瓶颈：28 分钟长文件单次 classify 仅 0.05–0.07s；ONNX/INT8/torch.compile
  已证伪禁用；CPU 推理线程数实测 **2 线程最优**（已写进部署脚本默认）。
- 省级权重 300 条 A/B：diting **1.922** vs 广西 1.915 vs 华南 1.916——打平。但该轮
  阈值是按 diting 调的，对省级权重不公平；广西域（官方样例）上必须各自搜阈值重赛（P1）。
  权重已入库：`weights/ustc_pickers/`（见 `weights/README.md`）。

## 2. 计划表（P1–P5）

| 阶段 | 前置 | 事项 | 平台 | 产出 / 验收 |
|---|---|---|---|---|
| **P1 样例数据获取 + 域复测** | 用户行动项 1（下载样例） | ① 样例画像：时长 / 台站数 / 通道构成（测震 vs HN 强震）/ gap / 事件密度 / 文件命名；② 重新 A/B：diting vs 广西 vs 华南（`weights/ustc_pickers/*_sd.pt`）vs 集成（P3 产物），**每个候选各自搜阈值后再比**（300 条打平的旧 A/B 阈值按 diting 调，不作数）；③ HN 加速度通道直接喂模型的效果实测 | 本地 CPU（推理评测=测试，允许） | 基座定案 + 样例域阈值定案；结论数值进 `defaults.py` |
| **P2 官方样例微调** | P1 画像完成 | Colab 上用官方样例（必要时 DiTing/CWA 补充）微调 diting。**50Hz 管线已就绪**：评审修复后 finetune 以 `model.sampling_rate` 为唯一真源、软标签按秒定义、holdout 分层抽样守门，100Hz/50Hz 错位与恒零守门两类致命 bug 已清 | Colab（本机禁训练） | best.pt；双闸门：留出集官方计分 + `scripts/ab_compare.py`（去年真题 + 官方样例） |
| **P3 新武器定装** | 与 P1/P2 并行（lab 在跑） | 多权重集成（`src/phasepicker/inference/ensemble.py`）、峰值精化、overlap 密度等实验，实验与结论在 `outputs/opt2/`；全部过 ab_compare，谁稳定提分谁写进默认 | 本地 CPU | `defaults.py` 数值更新或维持现状（负结论也留档） |
| **P4 震级模型升级** | 用户行动项 2（格式确认） | 保底：`/magnitude` 端点（serve_api 侧，输出格式做成可配置，官方格式一公布即对齐）；升级：SeisT-emg / MagNet 候选在 Colab 验证，用去年真题 T2 震级标签评测 | Colab 验证 + 本地推理 | 端点探活通过（`deploy/EVAL_DAY.md`）；升级模型过评测才替换保底 |
| **P5 多轮赛程运维** | 云主机到位（P0 遗留） | 每轮开赛前 T-1 重验、轮间迭代纪律（改动 → ab_compare 闸门 → 重部 → 外网复测）、watchdog 长期 cron、云主机余额/快照 | 云主机 | `deploy/EVAL_DAY.md` §6 多轮 checklist 每轮打勾留档 |

## 3. 风险与预案

| 风险 | 预案 |
|---|---|
| 样例数据迟迟未下载 | P1/P2/域复测全部空转——所以是置顶用户行动项；期间 P3（lab）与 P4 保底端点不受阻塞，先行 |
| 震级格式公布晚 / 与猜测不符 | /magnitude 输出格式做成可配置开关，公布当天切换即可；不押注任何未确认格式 |
| 评分细则口径变化（按震相总数 ≠ 按文件） | 数量罚口径变化最伤阈值——沿用 p3_grid 的"多读法全部改善才采纳"稳健准则；细则到手后重跑网格 |
| 长连续记录含 gap / 仪器混合域漂移 | 分段后短于一窗（61.02s）的片段会被 seisbench 静默跳过——`deploy/EVAL_DAY.md` T-1 有专项检查；域漂移靠 P1 复测 + P2 微调兜 |
| 多轮之间改动引入回归 | 任何改动必须 ab_compare + 外网复测双闸门；每轮开赛前 24h 冻结 |
| Colab 断线/限额 | checkpoint 全在 Drive，`--resume` 续训（管线已内置） |

## 4. 明确不做

- 本机不做任何训练/微调、不下大数据集（训练一律写成 Colab notebook / 云脚本入库，不执行）；
- 不做到时常数平移校正（去年两轮真题已证伪：最大联合收益 +0.0005 均分且 S 方向两轮相反）；
- 不用 ONNX/INT8/torch.compile（已证伪：不提速或损分）；不为速度牺牲任何一分；
- 不押注未公布的震级输出格式（可配置实现，不写死）。

---

## 留档：第一版计划（2026-07，基于"第一届赛制延续"假设）

> 以下为改版前原文。其中赛制假设（单事件短波形、开放约 1 天、初赛/复赛/决赛三段制）
> 已被上方第二届情报取代；P0 / P3a 的完成状态与结论仍然有效。数字与排期以新版为准。

### 留档 0. 赛制要点（决定计划形态）

| 项 | 官方要求 | 对策 |
|---|---|---|
| 提交方式 | 公网 HTTP API，比赛期间开放约 1 天 | `scripts/serve_api.py` 已按标准实现，部署即用 |
| 响应格式 | JSON：台站名→{"P":[UTC ISO],"S":[...]} | 已实现并有格式单测 + `check_api.py` 自检 |
| 初赛 | API 返回结果正常、符合要求 | **零训练即可过**：diting 预训练权重保底（基座 A/B 已定胜负，见 `deploy/README.md`） |
| 复赛 | 返回结果自动计分（P≤0.1s 满分/1s 零分；S≤0.2s/2s；数量误差>5% 每个−0.5） | 微调 + 阈值搜索是唯一大额分来源 |
| 决赛 | 复赛前 20 | 复赛分定生死 |
| 输入 | 100Hz 速度波形，无位置信息，含纯噪声条目 | 噪声条目输出空表；宁漏勿多报 |

### 留档 0.5 ⚡ 重大捷径（2026-07 调研确认，已落地到仓库）

- **基座换 `PhaseNet.from_pretrained('diting')`**：USTC 用 271 万条中国 DiTing 数据训好的
  CN picker，SeisBench 一行加载（几 MB）——**等于白拿整个 DiTing（原始 ~300GB）的知识，
  一个字节数据都不用下**。比 stead 跨域基线更贴广西赛题域。
- **USTC-Pickers 省级 picker**（github.com/JUNZHU-SEIS/USTC-Pickers，`model_list/v0.1/`）：
  中国分省转移学习权重，可用 `finetune_phasenet.py --init-weights` 作起点，或直接
  `serve_api.py --weights` 上线对比。（后续进展：广西/华南两套已入库
  `weights/ustc_pickers/`，300 条 A/B 与 diting 打平，待样例域复测。）
- **一键训练包**：`notebooks/colab_bigdata_finetune.ipynb` + `scripts/chunked_fetch.py`
  （分块下载→抽窗→删块，90GB 盘吃任意大集）+ `scripts/split_train_holdout.py`（按事件防泄漏切分）。
- **任何权重/开关切换先过 `scripts/ab_compare.py`**（去年真题上 A/B 同分/提分才启用）。

### 留档 1. 计划表

| 阶段 | 时间 | 事项 | 平台 | 产出 / 验收 |
|---|---|---|---|---|
| P0 保底上线（本地侧✅） | 第 0 天（半天） | 云主机装环境→`serve_api.py` 起服务（默认即 diting+新阈值，单一真源 `src/phasepicker/defaults.py`）→公网 `check_api.py` 全绿→平台登记 API 地址 | 阿里云/华为云 CPU（2~4 vCPU 够用） | 初赛达标线：外网可访问、格式全对。本地彩排全绿+部署包就绪，只差用户云主机 |
| P1 数据落位 | 第 1–2 天 | 跑 `colab_bigdata_finetune.ipynb` Cell 1–7：CWA 标注窗 8 万 + 噪声窗 1 万（分块循环，断点续传），按事件 90/10 切分 | Colab（产物镜像 Google Drive） | train/holdout HDF5，事件零交集 |
| P2 微调主线 | 第 2–6 天 | Cell 8：`--pretrained diting` 起步微调（BN 冻结 + holdout 官方计分守门 best）；实验矩阵：lr∈{3e-5,1e-4}×(diting / --init-weights 省级picker)×数据量；**以留出集官方计分选优，不看 loss** | Colab T4（单轮 1–2h，断线续训） | `best.pt` 若干；**验收/微调前后对比一律在新默认阈值 P=0.2/S=0.15 下评测，不得在 0.3 下比较**。目标校准（新阈值下损失版图已变）：S 漏检已从 28.7% 缩到 11.5%、P/S 损失比约 1:1.4——优先把线性区残差压进满分区（P≤0.1s 占比 75%→85%）并压残留 S 漏检，**勿再只攻 S 召回** |
| P3a 阈值搜索·基座（✅已完成 2026-08-01） | —（已前置完成，与 P2 零依赖） | 去年两轮真题全量网格搜索 diting 基座阈值：**P=0.2 / S=0.15 已写入 `src/phasepicker/defaults.py` 成为全局默认**（证据 outputs/probe 全量扫描：第1轮 1.629→1.717、第2轮 1.500→1.654，+0.09~0.15 均分；后续合并窗网格再 +0.015 至 1.732/1.669） | CPU | 已交付。残余细化与合并窗联合搜索见 outputs/p3_grid |
| P3b 阈值复搜·新权重 | P2 出 best.pt 后（1 天内） | 用留出集对微调权重复搜 P/S 阈值 × 去重合并窗，产出"稳健版/激进版"两套参数 | 任意 CPU | 两套 (threshold, merge_window)；仍偏高精确率（数量罚 −0.5/个 极重） |
| P4 上线冲刺 | 第 9–10 天 | ① `serve_api.py --weights best.pt` 换权重；② `--fp16` 开关：留出集同分才启用；③ 并发压测（连续 POST 2000 文件），P99 延迟 < 官方超时；④ systemd 自动重启 + `/health` 监控 | 云主机 | 复赛版 API；全量自检报告 |
| 持续 | 每天 | 代码/入选小权重/进度推 GitHub；大 checkpoint 训练中实时外送 Drive 或对象存储 | — | 任何机器随时可清零重来 |

### 留档 2. 分数模型（为什么这么排）

- **微调（P2）是最大单项**：P 波满分窗 0.1s；diting 基座在去年真题上匹配到的 P
  只有 ~75% 落进 0.1s 满分区、S 约 67–71% 落进 0.2s 满分区——把线性区残差压进
  满分区（每轮 36–70 分空间/相）是微调的主要提分面。
- **阈值搜索拆两半**：P3a（基座）已完成并兑现 +0.09~0.15 均分（纯配置零风险）；
  P3b 在 P2 新权重上复搜，防止微调改变概率分布后阈值错配。
- **速度不再是瓶颈**：合批+缓存后单文件延迟远低于评测超时；fp16/compile 只在
  "同分"验证后才开，速度永远让位于分数正确性。

### 留档 3. 风险与预案

| 风险 | 预案 |
|---|---|
| Colab 断线/限额 | 数据、checkpoint 全在 Drive；`--resume` 续训；GeoNet/DiTing 取数断点续传 |
| 微调训崩（历史踩坑：BN 被带偏） | 护栏已内置：BN 冻结 + holdout 官方计分守门 best + best 回滚；训后必跑微调前后对比（一律在新默认阈值 0.2/0.15 下） |
| 官方波形与训练域差异大 | 保留 stead 原始权重服务作 fallback；API 支持热切权重（重启 <1min） |
| 评测当天流量突发 | 单 worker + 推理互斥 + 预热已就位；压测过的 P99 为准；云主机选按量付费随时升配 |
| fp16 数值漂移 | 默认关闭；只有留出集逐分对比一致才开 |

### 留档 4. 明确不做

不从零训新架构（TCN/EQTransformer 集成放决赛展示层）；不下 STEAD 全量（跨境慢
且无必要）；不在本地跑任何训练；不为速度牺牲任何一分（所有加速开关先验分后启用）；
**不做到时常数平移校正**——2026-08-01 两轮真题实测已证伪：最大联合收益仅
+0.0005 均分，且 S 最优平移方向两轮相反（−0.02s vs +0.04s），从 P3 清单永久划掉。
