# 训练与提分计划表（2026 第二届"震智杯"·API 赛制）

> 前提：本仓库推理链路已优化到位（归档缓存、跨文件合批推理、I/O 流水线、
> 整批预测、fp16/满核开关、常驻模型 API 服务）。剩下的分数只能靠**数据与微调**拿。
> 本计划全部在云端（Colab / 云 GPU）执行，不在本地训练、不在本地下大数据集。

## 0. 赛制要点（决定计划形态）

| 项 | 官方要求 | 对策 |
|---|---|---|
| 提交方式 | 公网 HTTP API，比赛期间开放约 1 天 | `scripts/serve_api.py` 已按标准实现，部署即用 |
| 响应格式 | JSON：台站名→{"P":[UTC ISO],"S":[...]} | 已实现并有格式单测 + `check_api.py` 自检 |
| 初赛 | API 返回结果正常、符合要求 | **零训练即可过**：stead 预训练权重保底 |
| 复赛 | 返回结果自动计分（P≤0.1s 满分/1s 零分；S≤0.2s/2s；数量误差>5% 每个−0.5） | 微调 + 阈值搜索是唯一大额分来源 |
| 决赛 | 复赛前 20 | 复赛分定生死 |
| 输入 | 100Hz 速度波形，无位置信息，含纯噪声条目 | 噪声条目输出空表；宁漏勿多报 |

## 0.5 ⚡ 重大捷径（2026-07 调研确认，已落地到仓库）

- **基座换 `PhaseNet.from_pretrained('diting')`**：USTC 用 271 万条中国 DiTing 数据训好的
  CN picker，SeisBench 一行加载（几 MB）——**等于白拿整个 DiTing（原始 ~300GB）的知识，
  一个字节数据都不用下**。比 stead 跨域基线更贴广西赛题域。
- **USTC-Pickers 省级 picker**（github.com/JUNZHU-SEIS/USTC-Pickers，`model_list/v0.1/`）：
  中国分省转移学习权重，可用 `finetune_phasenet.py --init-weights` 作起点，或直接
  `serve_api.py --weights` 上线对比。
- **一键训练包**：`notebooks/colab_bigdata_finetune.ipynb` + `scripts/chunked_fetch.py`
  （分块下载→抽窗→删块，90GB 盘吃任意大集）+ `scripts/split_train_holdout.py`（按事件防泄漏切分）。
- **任何权重/开关切换先过 `scripts/ab_compare.py`**（去年真题上 A/B 同分/提分才启用）。

## 1. 计划表

| 阶段 | 时间 | 事项 | 平台 | 产出 / 验收 |
|---|---|---|---|---|
| P0 保底上线 | 第 0 天（半天） | 云主机装环境→`serve_api.py --pretrained diting` 起服务→公网 `check_api.py` 全绿→平台登记 API 地址（`diting` vs `stead` 先用 ab_compare 在去年真题上定胜负） | 阿里云/华为云 CPU（2~4 vCPU 够用） | 初赛达标线：外网可访问、格式全对 |
| P1 数据落位 | 第 1–2 天 | 跑 `colab_bigdata_finetune.ipynb` Cell 1–7：CWA 标注窗 8 万 + 噪声窗 1 万（分块循环，断点续传），按事件 90/10 切分 | Colab（产物镜像 Google Drive） | train/holdout HDF5，事件零交集 |
| P2 微调主线 | 第 2–6 天 | Cell 8：`--pretrained diting` 起步微调（BN 冻结 + holdout 官方计分守门 best）；实验矩阵：lr∈{3e-5,1e-4}×(diting / --init-weights 省级picker)×数据量；**以留出集官方计分选优，不看 loss** | Colab T4（单轮 1–2h，断线续训） | `best.pt` 若干；目标：留出集 P≤0.1s 命中率 ≥85% |
| P3 阈值/后处理搜索 | 第 7–9 天 | 用本地评分器在留出集网格搜索：P/S 概率阈值 × 去重合并窗；偏高精确率（数量罚 −0.5/个 极重）；产出"稳健版/激进版"两套参数 | 任意 CPU | 两套 (threshold, merge_window) 参数；此阶段通常再涨 5–15% |
| P4 上线冲刺 | 第 9–10 天 | ① `serve_api.py --weights best.pt` 换权重；② `--fp16` 开关：留出集同分才启用；③ 并发压测（连续 POST 2000 文件），P99 延迟 < 官方超时；④ systemd 自动重启 + `/health` 监控 | 云主机 | 复赛版 API；全量自检报告 |
| 持续 | 每天 | 一切产物（代码/权重/进度）推 Gitee；checkpoint 训练中实时外送 Drive | — | 任何机器随时可清零重来 |

## 2. 分数模型（为什么这么排）

- **微调（P2）是最大单项**：P 波满分窗 0.1s=10 个采样点，stead 权重跨域残差常在
  0.1–0.3s，掉进线性区甚至零分区；微调把残差中位数压进 0.05s 内即接近满分区。
- **阈值搜索（P3）是第二大项且零风险**：数量罚 −0.5/个，阈值每调 0.05 都直接改变
  漏报/误报平衡，必须由留出集分数反推，不拍脑袋。
- **速度不再是瓶颈**：合批+缓存后单文件延迟远低于评测超时；fp16/compile 只在
  "同分"验证后才开，速度永远让位于分数正确性。

## 3. 风险与预案

| 风险 | 预案 |
|---|---|
| Colab 断线/限额 | 数据、checkpoint 全在 Drive；`--resume` 续训；GeoNet/DiTing 取数断点续传 |
| 微调训崩（历史踩坑：BN 被带偏） | 护栏已内置：BN 冻结 + 合成集守底 + best 回滚；训后必跑微调前后对比 |
| 官方波形与训练域差异大 | 保留 stead 原始权重服务作 fallback；API 支持热切权重（重启 <1min） |
| 评测当天流量突发 | 单 worker + 推理互斥 + 预热已就位；压测过的 P99 为准；云主机选按量付费随时升配 |
| fp16 数值漂移 | 默认关闭；只有留出集逐分对比一致才开 |

## 4. 明确不做

不从零训新架构（TCN/EQTransformer 集成放决赛展示层）；不下 STEAD 全量（跨境慢
且无必要）；不在本地跑任何训练；不为速度牺牲任何一分（所有加速开关先验分后启用）。
